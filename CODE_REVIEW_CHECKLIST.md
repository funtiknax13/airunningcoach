# Чек-лист код-ревью (2026-08-29)

Полное ревью всего закоммиченного кода (backend + frontend-v2 + CI/nginx),
5 параллельных агентов. 31 находка, отсортировано по приоритету — сверху вниз.

---

## 🔴 Критично — security, фиксить первым

- [x] **Payment webhook принимает подделанные запросы** — `backend/app/routers/payments.py:97`
  Нет проверки подписи YooKassa; если `Payment` не найден, создаётся новый со статусом `succeeded` прямо из `metadata.user_id`/`plan` в теле запроса.
  → Любой может POST'нуть фейковый `payment.succeeded` и бесплатно выдать себе (или кому угодно) год Premium.
  ✅ Фикс: вебхук больше не доверяет телу запроса — сервер сам проверяет статус через `yookassa.Payment.find_one()` (тот же паттерн, что уже был в `/payments/verify`). Проверено повтором атаки — `502 Verification failed`, Premium не выдаётся.

- [x] **`SECRET_KEY` по умолчанию — публичный, без проверки при старте** — `backend/app/core/config.py:7`
  Дефолт закоммичен в репозиторий, ничего не падает при старте, если env-переменная не задана.
  → Если `SECRET_KEY` не задан в проде — можно подделать JWT для любого `user_id`, включая админов.
  ✅ Фикс: pydantic-валидатор роняет приложение при старте, если ключ всё ещё дефолтный. Проверено: с дефолтом — падает, с реальным ключом — стартует чисто.

- [x] **XSS в чате: AI-сообщения рендерятся через `v-html` без санитайзера** — `frontend-v2/src/components/chat/ChatBox.vue:13`
  `marked.parse(content)` без DOMPurify (нигде в проекте не подключён).
  → Если модель когда-нибудь отдаст `<img onerror=...>` (prompt injection через заметки/имена файлов), это выполнится в сессии жертвы — токен лежит в localStorage, значит это ещё и угон токена.
  ✅ Фикс: добавлен DOMPurify, вынесен в общий `utils/markdown.ts` (дубль в `ChatBox.vue`/`CoachPage.vue` убран). Проверено реальными пейлоадами (`<script>`, `onerror=`, `javascript:`-ссылки — все вырезаются, обычный markdown рендерится как раньше).

---

## 🟠 Высокий — реальные баги/инфраструктурные риски

- [x] **`alembic env.py` не знает про 7 боевых таблиц** — `backend/alembic/env.py:15` — **оказалось ложным срабатыванием**, не трогал
  Перепроверил перед тем, как чинить: `from app.models import X, Y, ...` в Python исполняет ВЕСЬ модуль `models.py` целиком (декларативный класс SQLAlchemy регистрируется на `Base.metadata` в момент ОПРЕДЕЛЕНИЯ класса, не в момент импорта конкретного имени) — то есть `Base.metadata.tables` реально видит все 14 таблиц независимо от того, что явно перечислено в строке импорта. Проверил напрямую: `Base.metadata.tables.keys()` с тем же импортом, что в `env.py`, — все 14 таблиц на месте. `autogenerate` ничего не уронит.

- [x] **`alembic upgrade head` с нуля падает** — `backend/alembic/versions/0010_read_seen.py:27`
  Таблицы `personal_records`/`user_achievements`/`push_subscriptions` нигде не создаются миграциями — только через `Base.metadata.create_all()` в `main.py`. `0010` делает `ADD COLUMN` на несуществующую (по мнению alembic) таблицу.
  → Новая база (staging, CI, восстановление после аварии) не поднимется через `alembic upgrade head`.
  ✅ Фикс: `0010` теперь defensively создаёт `user_achievements`, если её ещё нет (безопасно для прода — alembic не перевыполняет уже применённые миграции). Новая миграция `0017_untracked_schema.py` добивает `personal_records`/`push_subscriptions` + все колонки/FK, которые раньше жили только в `_migrate()`. Проверено: полный прогон `alembic upgrade head` с абсолютного нуля на свежем Postgres (все 17 миграций подряд, включая 0010) — успешно, схема совпадает с моделями 1:1 (проверил колонки и ON DELETE на FK). Идемпотентность проверена повторным прогоном (no-op) и накатом на существующую (уже заполненную) локальную БД.

- [x] **Параллельный недокументированный путь миграций мимо alembic** — `backend/app/main.py:40`
  `_migrate()` руками гоняет `ALTER TABLE ADD COLUMN IF NOT EXISTS` для полей, которых нет ни в одной alembic-миграции.
  → История миграций больше не воспроизводит реальную схему с нуля.
  ✅ Фикс: `Base.metadata.create_all()` и `_migrate()`/`_fk_ondelete_sql()` убраны из `main.py` целиком — вся эта логика теперь в `0017_untracked_schema.py`. Alembic — единственный источник схемы.

- [x] **CORS: wildcard + `allow_credentials=True`** — `backend/app/main.py:82`
  Известная плохая комбинация — миддлварь отражает любой `Origin` как доверенный.
  → Сейчас не критично (авторизация на bearer-токене), но станет дырой в момент появления cookie-based флоу.
  ✅ Фикс: проверил, что реальный трафик всегда same-origin (nginx в проде, Vite-прокси локально — `credentials` нигде не используется в `api/client.ts`) — заменил на явный allowlist origin'ов + `allow_credentials=False`. Проверено: разрешённый origin получает `access-control-allow-origin`, произвольный (`evil.com`) — нет; реальная регистрация через nginx по-прежнему работает.

- [x] **Service Worker кэш никогда не обновляется между деплоями** — `frontend-v2/public/sw.js:1`
  `CACHE = 'runcoach-v1'` — статичная строка, не связана с `__BUILD_ID__`.
  → После деплоя у вернувшегося пользователя может отдаться закэшированный HTML со ссылками на удалённые хэшированные JS/CSS файлы (404) — приложение ломается до хард-рефреша.
  ✅ Фикс: build id теперь подставляется в `dist/sw.js` отдельным пост-билд шагом (`scripts/inject-sw-version.mjs`, вызывается после `vite build`) — проверено, что версия реально меняется между сборками. Дополнительно: навигации (сам HTML) теперь всегда идут network-first (кеш — только офлайн-фоллбэк), а не cache-first, плюс `controllerchange`-листенер в `main.ts` перезагружает открытые вкладки при обновлении SW.

- [x] **Нельзя сохранить тренировку с нулевой дистанцией** — `frontend-v2/src/components/activities/ActivityModal.vue:93`
  `!form.value.distance_km` считает `0` отсутствием значения.
  → Силовая/другая тренировка с дистанцией 0 не сохраняется вообще.
  ✅ Фикс: заменил на явную проверку `== null` вместо falsy-проверки.

- [x] **Онбординг: ошибка сохранения профиля проглатывается молча** — `frontend-v2/src/pages/OnboardingPage.vue:173`
  Нет `try/catch`, нет отображения ошибки, необработанный promise rejection.
  → При сетевом сбое пользователь видит, что кнопка разблокировалась, и никакой обратной связи.
  ✅ Фикс: добавлен `try/catch` + видимое сообщение об ошибке под кнопкой.

- [x] **Race condition: логаут может "воскресить" юзера в сторе** — `frontend-v2/src/stores/auth.ts:27`
  Фоновый `syncTimezone()` не отменяется/не ждётся; если ответ приходит после `logout()`, он молча перезаписывает `user.value`.
  → Возможна вспышка данных вышедшего пользователя в UI.
  ✅ Фикс: `.then()` теперь проверяет, что `user.value?.id` всё ещё совпадает с тем, для кого запрос отправлялся (закрывает и логаут, и вход другим пользователем, а не только логаут).

- [x] **Нет теста/линта перед деплоем на прод** — `.github/workflows/deploy.yml`
  Единственный пре-деплой шаг — `npm run build` фронта; для бэкенда нет вообще ничего.
  → Синтаксическая ошибка в бэкенде уедет прямо в прод, первым сигналом будет падение приложения.
  ✅ Фикс: реального pytest-набора в проекте нет (писать с нуля — отдельная большая задача, не в рамках этого фикса), но добавил дешёвый и высокоценный gate — `py_compile` по всем файлам + реальный импорт `from app.main import app` (ловит не только синтаксис, но и сломанные импорты и ошибки на уровне модуля, включая новый SECRET_KEY-валидатор). Проверено локально: на валидном коде проходит, на нарочно сломанном синтаксисе — падает с exit 1. Заодно поправил соседний баг в том же файле — `git diff HEAD~1 HEAD` для определения "менялся ли backend" смотрел только на последний коммит пуша, теперь сравнивает с `github.event.before` (весь диапазон пуша).

---

## 🟡 Средний

- [ ] **SSRF: DNS rebinding в проверке внешних URL** — `backend/app/services/safe_fetch.py:24`
  Валидация резолвит хост и проверяет IP, но реальный запрос резолвит DNS заново — провалидированный IP нигде не закрепляется.
  → При контроле над DNS можно подсунуть внутренний адрес после прохождения проверки (через импорт активности по ссылке).

- [ ] **Strava webhook принимает неаутентифицированные события** — `backend/app/routers/strava_webhook.py:31`
  Нет проверки, что запрос реально от Strava (кроме одноразового GET-хендшейка).
  → Зная `owner_id` (Strava athlete ID, часто виден в публичном профиле) и `strava_id`, можно удалённо удалить чужую активность.

- [ ] **`check_and_record` — гонка "проверил-потом-записал"** — `backend/app/services/rate_limit.py:94`
  `SELECT COUNT < limit` → `INSERT` без блокировки строки.
  → Параллельные запросы (два таба, дабл-тап) могут пробить лимит на количество одновременных запросов.

- [ ] **Payment webhook: гонка на повторной доставке события** — `backend/app/routers/payments.py:139`
  Проверка "уже обработано" и продление Premium не защищены блокировкой строки.
  → Два одновременных ретрая одного и того же события — last-write-wins вместо честной идемпотентности.

- [ ] **Сброс токена верификации без подтверждения владения** — `backend/app/routers/auth.py:104`
  Если `password` не передан, `resend-verification` работает по одному email.
  → Зная чужой email, можно инвалидировать письмо верификации, которое жертва вот-вот откроет (грифинг).

- [ ] **Сравнение naive/aware datetime при подборе тренировки под план** — `backend/app/services/workout_verification.py:138`
  `planned_date` (наивная, локальная дата) сравнивается с `Activity.date` (aware UTC) без конвертации — соседняя функция такую же ошибку явно чинит комментарием.
  → При большом смещении UTC окно подбора "лучшей" активности может сдвинуться на несколько часов.

- [ ] **Та же naive/aware проблема в еженедельной статистике** — `backend/app/services/trial_emails.py:159`
  `now`/`next_week_end` (aware UTC) сравниваются с `Workout.planned_date` (наивная).
  → Список "предстоящих тренировок" в письме может терять/дублировать записи у пользователей не в UTC.

- [ ] **30-дневное окно инсайтов использует наивное серверное время** — `backend/app/routers/ai_insights.py:26`
  `datetime.now()` без `timezone.utc`, в отличие от остальных мест в коде.
  → Если сервер не в UTC — окно "последние 30 дней" не совпадает с тем же окном в других эндпоинтах.

- [x] **CI: определение "менялся ли backend" не видит все коммиты пуша** — `.github/workflows/deploy.yml:25`
  `git diff HEAD~1 HEAD` сравнивает только с непосредственным родителем.
  → При пуше из нескольких коммитов бэкенд может не пересобраться, хотя код в нём менялся.
  ✅ Фикс: заодно с добавлением test gate (см. 🟠) — сравнение теперь с `github.event.before` (весь диапазон пуша), `fetch-depth: 0` вместо `2` (нужна полная история для диффа против произвольно старого `before`).

- [ ] **TTL кэша инсайтов противоречит своему же докстрингу** — `backend/app/services/insights_cache.py:12`
  Комментарии говорят "2 часа", константа — `timedelta(hours=24)`.
  → Если 2ч — задуманное поведение, инсайты могут быть протухшими на 24ч там, где `invalidate_insights_cache()` забыли вызвать.

- [ ] **Роутер не перепроверяет авторизацию после первой загрузки** — `frontend-v2/src/router/index.ts:29`
  `loadMe()` вызывается один раз; протухший/отозванный JWT не ловится гвардом.
  → Открытая вкладка после истечения токена продолжает выглядеть залогиненной, пока какой-то запрос не словит 401.

- [ ] **Off-by-one день у даты цели для пользователей западнее UTC** — `frontend-v2/src/components/goals/GoalModal.vue:93`
  `new Date(YYYY-MM-DD).toISOString()` трактует дату как UTC-полночь, а отображается в локальном времени зрителя.
  → В основном бьёт по en-локали/не-РФ пользователям (продукт заточен под положительные смещения).

---

## ⚪ Низкий / edge-case / мёртвый код

- [ ] **Push-подписку можно тихо переприсвоить другому пользователю** — `backend/app/routers/push.py:20`
  Поиск существующей подписки идёт по `endpoint`, без привязки к текущему юзеру.
  → Низкая эксплуатируемость (endpoint обычно непредсказуем), но IDOR-паттерн налицо.

- [ ] **Strava dedup по временному окну может перезаписать чужую активность** — `backend/app/routers/strava_webhook.py:90`
  Новое `create`-событие ищет "любую" активность в ±1 минуту и переписывает её `strava_id`.
  → Две близкие по времени активности (брик-тренировка, два синхронизирующихся девайса) — вторая тренировка теряется.

- [ ] **`_is_rate_limit()` — хрупкое совпадение по подстроке "429"** — `backend/app/services/ai_agent.py:135`
  Фолбэк `" 429" in f" {e}"` сработает на любой ошибке, где "429" случайно встретилось в тексте.

- [ ] **Сплиты по км могут тихо показать `0:00/км`** — `backend/app/services/gpx_parser.py`
  `pace = elapsed_min / seg_dist` без проверки `elapsed_min > 0` при дублирующихся/невозрастающих таймстампах.

- [ ] **Мёртвый компонент с логаутом-миной** — `frontend-v2/src/components/layout/AppHeader.vue:12`
  Нигде не используется; если когда-нибудь подключат — логаут не очищает Pinia-сторы (только localStorage-кэш).

- [ ] **Falsy-zero прячет настоящие нулевые значения статистики** — `frontend-v2/src/components/activities/ActivityDetail.vue:10`
  `v-if="detail.elevation_gain"` и подобные — честный `0` (плоская трасса) выглядит как "нет данных".

- [ ] **В аварийном nginx-конфиге устаревший путь до админки** — `backend/nginx/nginx.conf:46`
  Docker-нginx (документированный путь отката) проксирует `/admin`, но SQLAdmin реально висит на `/sqladmin`.
  → Если когда-нибудь реально придётся откатиться на этот конфиг — админка станет недоступна именно в момент аварии.

---

*Сгенерировано по итогам код-ревью пяти параллельных агентов (backend routers/auth, backend services, backend core/models/migrations/CI, frontend static/config, frontend Vue app).*
